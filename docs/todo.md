# TODO

- 2026-06-03: Не запускать автоматическую синхронизацию, когда MacBook работает от аккумулятора (питание не подключено). Ручной запуск пользователем должен оставаться доступным; учесть `IOPowerSources` / `NSProcessInfo.isLowPowerModeEnabled` и проверку наличия AC adapter. Возможно, вынести в `AppPreferences` опцию "Sync only on AC power" (по умолчанию включена).
- 2026-06-03: Если GeoIP определяет страну, отличную от заданной пользователем «домашней», показывать предупреждение и требовать явного подтверждения перед синхронизацией. Цель — не гонять большой трафик по дорогому или лимитированному каналу в поездке. Нужен источник GeoIP (например, обращение к публичному API при старте сессии), настройка ожидаемой страны в `AppPreferences`, и UX-подтверждение в `PushEligibilityPolicy` / UI слое.
